@model List<Club> 

<section>
    <div class="container">
        @foreach(var item in Model)
        {
            <div class="row">
                <div class="col-6">
                    <div class="text-center">
                        @item.Title
                    </div>
                </div>

            </div>
        }
        
    </div>
</section>